# Pinned native environment for Apple Silicon
# Phase 7.26: Create pinned native environment for Apple Silicon
# This file pins the exact MLX and MLX-LM versions specified in release.toml
# for reproducible Apple Silicon validation

# Core dependencies (from pyproject.toml)
numpy==1.26.4
matplotlib>=3.8
requests>=2.31
pydantic>=2.5
pyyaml>=6.0.1

# MLX dependencies (pinned to release.toml versions)
mlx==0.21.1
mlx-lm==0.20.6

# Optional dependencies for testing
pytest>=7.0,<9
pytest-timeout>=2.3
pytest-asyncio>=0.23
pytest-cov>=5.0
ruff==0.4.*
mypy==1.8.*

# Optional dependencies for memory features
qdrant-client>=1.9
sentence-transformers>=3.0

# Optional dependencies for production
huggingface_hub>=0.20
psutil>=5.9
structlog>=24.1
fastapi>=0.110
uvicorn[standard]>=0.29
